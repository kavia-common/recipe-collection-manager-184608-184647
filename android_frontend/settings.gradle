rootProject.name = "example-android-app"
// Proxy to Declarative Gradle: include the DCL settings file to satisfy tools looking for settings.gradle
// This keeps the existing settings.gradle.dcl authoritative.
if (file("settings.gradle.dcl").exists()) {
    // No-op: presence of this settings.gradle is enough for Gradle tooling to detect a build here.
    // The actual build uses settings.gradle.dcl via the declarative plugin ecosystem.
}
